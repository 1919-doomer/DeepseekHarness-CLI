# Initial issue plan

The following parent issues should exist in GitHub and remain the live milestone trackers:

1. M0 — Contract and architecture lock
2. M1 — Runtime vertical slice
3. M2 — Interactive terminal loop
4. M3 — TUI product layer
5. M4 — Safety, reliability and compatibility
6. M5 — Public alpha

Each parent issue should link back to `docs/ROADMAP.md` and contain a checklist matching its exit criteria. Implementation PRs should close child issues rather than editing this document as a TODO list.
