# Progress reporting policy

Project progress should be reported from GitHub state, not estimated from chat activity.

A milestone is complete when its documented exit criteria are met on `main`. Work in progress should be represented by open GitHub Issues and Pull Requests. If a task is blocked by an upstream DeepSeek Harness change, record the blocker in the relevant Issue.

Avoid arbitrary percentage-complete claims. Prefer milestone/task states backed by acceptance criteria.
